[build]
  command = "npm run pre-build && npm run build"
  publish = "dist/chambea/browser"
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/json-server"
  status = 200
  force = true
